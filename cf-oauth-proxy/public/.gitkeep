This directory intentionally has no static assets — this Pages project only
serves the Functions under functions/. Cloudflare Pages still requires an
output directory to be configured, hence this placeholder.
