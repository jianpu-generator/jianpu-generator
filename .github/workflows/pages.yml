name: Deploy web to GitHub Pages

on:
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10.12.1

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: web/pnpm-lock.yaml

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown

      - name: Install dependencies
        working-directory: web
        run: pnpm install --frozen-lockfile

      - name: Build
        working-directory: web
        env:
          VITE_BASE_PATH: /jianpu-generator/
          # Deployed URL of the step-1 Cloudflare Pages OAuth proxy
          # (`cf-oauth-proxy/`), which relays the two GitHub device-flow
          # calls that don't send CORS headers. Placeholder below — replace
          # with the real project URL once that Cloudflare Pages project is
          # deployed (see `cf-oauth-proxy/README.md`).
          VITE_GITHUB_OAUTH_PROXY_URL: https://jianpu-oauth-proxy.pages.dev
          # The OAuth App's client ID. Not a secret (GitHub's device flow
          # sends it in plaintext), but functionally unused by the proxy
          # above, which injects the real `GITHUB_CLIENT_ID` server-side
          # regardless of what's sent here — required only because
          # `@octokit/auth-oauth-device` needs a non-empty `clientId` to
          # construct its requests. Replace with the real OAuth App's client
          # ID for clarity/future-proofing once one is registered.
          VITE_GITHUB_OAUTH_CLIENT_ID: placeholder-client-id
          # PartyKit/Cloudflare Worker host for live-share sessions. Not a
          # secret (exposed client-side regardless), but only ever set in
          # the gitignored `web/.env.local` for local dev, so it's absent
          # here unless declared explicitly.
          VITE_PARTYKIT_HOST: jianpu-live.hou32hou.workers.dev
        run: pnpm build

      - name: Add .nojekyll
        run: touch web/dist/.nojekyll

      - uses: actions/upload-pages-artifact@v3
        with:
          path: web/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/deploy-pages@v4
        id: deployment

  deploy-cloudflare:
    runs-on: ubuntu-latest
    environment:
      name: cloudflare-pages
      url: https://jianpu-generator.pages.dev
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10.12.1

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: web/pnpm-lock.yaml

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown

      - name: Install dependencies
        working-directory: web
        run: pnpm install --frozen-lockfile

      # Rebuilt separately from the GitHub Pages `build` job above because
      # Cloudflare Pages serves from the domain root, so this needs
      # VITE_BASE_PATH=/ baked in rather than /jianpu-generator/.
      - name: Build
        working-directory: web
        env:
          VITE_BASE_PATH: /
          VITE_GITHUB_OAUTH_PROXY_URL: https://jianpu-oauth-proxy.pages.dev
          VITE_GITHUB_OAUTH_CLIENT_ID: placeholder-client-id
          VITE_PARTYKIT_HOST: jianpu-live.hou32hou.workers.dev
        run: pnpm build

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: web
          command: pages deploy dist --project-name=jianpu-generator --branch=master
